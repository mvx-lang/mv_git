# Derived build artifacts (build-native.sh / mkpkg output)
CATALOG/
CATALOG.DICT/
LIB/
LIB.DICT/
bin/
mvxdata.lmdb/
BP.O/
BP.O.DICT/
*.dSYM/
*.o
*.dylib
*.so
udt-callc/*.o
# PLATFORM.H is a per-platform build artifact (mkpkg.sh / build-udt.sh write it).
# Matched at any depth: the generators write it both at the package root and
# beside the BP sources, and a pattern containing a slash is anchored to this
# directory, so the anchored form missed the second copy.
**/BP.INC/PLATFORM.H
# Generated by build-gitd.sh from BP/GIT.AGENT (embedded into uv-git).
src/agent_src.h
