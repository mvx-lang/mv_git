# Derived build artifacts (build-native.sh / mkpkg output)
CATALOG/
CATALOG.DICT/
LIB/
LIB.DICT/
bin/
mvxdata.lmdb/
BP.O/
BP.O.DICT/
*.dSYM/
*.o
*.dylib
*.so
udt-callc/*.o
# PLATFORM.H is a per-platform build artifact (mkpkg.sh / build-udt.sh write it),
# generated both at the package root and beside the BP sources — the pattern is
# unanchored so it covers BP.INC/ and BP/BP.INC/ alike.  (main's broader form:
# it also catches the root copy the branch's anchored pattern missed.)
**/PLATFORM.H
# Generated by embed-agent.sh from BP/GIT.AGENT (embedded into uv-git/udt-git).
src/agent_src.h

# The VOC's own dictionary is the platform's, not the account's - every
# account is born with it, and a clone gets its own (mv_git#95).
VOC.DICT/

# Derived verb registration: mkpkg writes it from VERBS (mvx#136).
/VOC/GIT
