{
  "name": "mvx-lang/git",
  "version": "1.2.2",
  "description": "git for hash-file records (branch/merge)",
  "license": "GPL-2.0-only",
  "systems": ["mvx", "udt"],
  "dependencies": ["mvx-lang/cmd"],
  "deploy": {
    "verbs": ["GIT"],
    "fallback": { "cmd": ["CMD.BP"] }
  }
}
