{
  "name": "mv-lang/git",
  "version": "1.0.1",
  "description": "git for hash-file records (branch/merge)",
  "license": "GPL-2.0-only",
  "systems": ["mvx", "udt"],
  "dependencies": ["mv-lang/cmd"],
  "deploy": {
    "verbs": ["GIT"],
    "fallback": { "cmd": ["CMD.BP"] }
  }
}
